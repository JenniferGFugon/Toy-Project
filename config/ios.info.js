class IosInfo {
    static deviceName() {
        return 'iPhone XS'; // pass the device name
    }

    static platFormVersion() {
        return '13.2'; // pass the platform version
    }

    static appName() {
        return 'com.apple.Preferences'; // pass the bundleid or ipa name
    }
}

module.exports = IosInfo;
